#pragma once

//-----------------------------------------------------------------------------------------
// include
//-----------------------------------------------------------------------------------------
#include "Random.hlsli"

////////////////////////////////////////////////////////////////////////////////////////////
// Random namespace
////////////////////////////////////////////////////////////////////////////////////////////
namespace Random {

	////////////////////////////////////////////////////////////////////////////////////////////
	// Engine structure
	////////////////////////////////////////////////////////////////////////////////////////////
	struct Engine {

		//=========================================================================================
		// public variables
		//=========================================================================================

		uint3 seed;

		//=========================================================================================
		// public methods
		//=========================================================================================

		uint Rand1d() {
			seed.x = Random::Xorshift::Rand(seed.x);
			return seed.x;
		}

		uint2 Rand2d() {
			seed.xy = Random::Xorshift::Rand(seed.xy);
			return seed.xy;
		}

		uint3 Rand3d() {
			seed.xyz = Random::Xorshift::Rand(seed.xyz);
			return seed.xyz;
		}

	};

}
